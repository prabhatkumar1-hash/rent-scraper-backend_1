services:
  - type: web
    name: rent-backend
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt && ./render_postdeploy.sh"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
